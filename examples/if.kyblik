func testIf {
  input(num: number)
  output(num)

  // comment
  /* comment 2 */

  if (isNumber(num)) {
    log('It is number indeed!')
    num = num {"* 3"}
  } else {
    log('It is not number \(._.)/!')
    num = random(0, 25)
  }
}(callable as *1)

newNum = *1(3)
log(newNum)
